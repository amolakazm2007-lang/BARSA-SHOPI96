# BARSA SHOPI v4.8 — Completion Pass

## Added in this pass
- Dedicated engine navigation: Quality / Face AI / Blur Studio / DaVinci Mini.
- Blur Studio is now a standalone tab while preserving the existing temporal MotionBlurEngine and RIFE path.
- Dedicated MP4 Export Studio with Smart/Maximum/Custom video bitrate, AAC bitrate, and hardware/software encoder preference.
- Export choices are wired into VideoPipeline; custom bitrate is bounded to 240 Mbps and audio to 320 kbps.
- Spectral Prestige mobile theme (acid green / cyan / violet / magenta), stronger hierarchy, compact mobile navigation.
- Existing H.264 + AAC, yuv420p, Fast Start, A/V sync and MP4 validation remain in the pipeline.

## Verification
- `node --check`: PASS for modified JavaScript modules.
- `npm run check`: PASS.
- `npm run final:audit`: PASS (42 source modules, 14 critical files + runtime asset hash/size checks).
- `npm test`: 83/86 PASS. The 3 failures are dependency-environment failures because `mediabunny` and `playwright` are absent from this unpacked runtime; they are not assertion failures in BARSA engine logic.

## Readiness boundary
This remains a Vite/WebCodecs/WebGPU engine build. A production Android APK still requires the native Android/Capacitor shell, Android file/save integration, lifecycle/thermal testing, and real POCO F6 acceptance tests.
