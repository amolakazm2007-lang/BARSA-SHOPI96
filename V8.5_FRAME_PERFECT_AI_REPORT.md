# BARSA SHOPI v8.5 — FRAME PERFECT AI

## هدف الإصدار
منع إسقاط/تخطي الفريمات بصمت ورفع التقوية الشاملة لتشمل مراحل Quality V2 وFace V2.

## Frame-perfect render
- Mediabunny sequential demux/decode is mandatory in strict mode.
- HTMLVideo random seeking fallback is blocked by default because it may silently repeat/skip frames.
- New constant-memory `FrameIntegrityMonitor` accounts for decoded -> processed -> encoded frames.
- Strict non-monotonic source detection.
- Zero missing output timestamps allowed.
- Zero irregular-ratio tolerance in final CFR pacing validation.
- Encoder submission count must equal durable elementary packet count before mux.
- MP4 mux/fallback remains protected by the v8.1 durable elementary stream path.
- Timeline gaps from legitimate VFR/edit-list timing are recorded; the CFR sequencer fills the output timeline rather than silently dropping output points.

## AI Power Pass
All Engine Boost now also covers:
- Micro Texture V2
- Structure Recovery V2
- stronger Fine Detail / Texture / Edge Recovery
- Face Detail
- Skin Cleanup
- Face Micro Contrast V2
- Skin Tone Protect V2
- Temporal Reconstruction enable/strength

Model selection remains manual. No content-based automatic model selection was enabled.

## Blur reference
The Blur workspace continues to follow the documented concepts of the official `f0e/blur` repository:
https://github.com/f0e/blur?tab=readme-ov-file

## Verification
- v8.5 new tests: 5/5 PASS
- full suite: 138 tests / 135 PASS / 3 environment dependency failures
- Source Policy: PASS
- Final Audit: PASS
- UI Audit: PASS
- 51 source modules
- 14 critical runtime files audited
- Android: versionCode 85 / versionName 8.5.0

The three non-running tests require packages absent in the current local session (`mediabunny` and Playwright runtime). They are unrelated to the new frame-integrity assertions.

## Physical-device note
True zero-drop verification under long 1080p/4K NNAPI workloads still requires installing the generated APK on the target POCO F6 and measuring the real render. The source now fails closed instead of silently producing an output when its strict frame-accounting invariants are violated.
