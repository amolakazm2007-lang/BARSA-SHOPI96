# Engine test report

التقرير الحالي الموثق ونتائج الاختبار الفعلي موجودة في:

`docs/CURRENT_PROJECT_AUDIT.md`

الأوامر المعتمدة:

```bash
npm run check
npm test
npm run build
```

آخر نتيجة: 53 اختباراً ناجحاً، تشمل Geometry لكل الأبعاد والاتجاهات، ملف POCO F6، مستويات H.264 حتى 8K، demux مباشر، Scene Cut Guard، RIFE/CodeFormer signature adapters، Temporal Denoise، SHA-256 المتدرج، Mobile SR/fixed-tile inference، وبث وفحص MP4/H.264 حقيقي، مع build إنتاجي ناجح.
