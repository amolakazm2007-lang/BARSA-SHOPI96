import test from'node:test';import assert from'node:assert/strict';import{applyRealtimeEffects}from'../src/engine/RealtimePreviewEngine.js';
test('preview processor changes pixels and preserves alpha',()=>{const width=5,height=5,data=new Uint8ClampedArray(width*height*4);for(let y=0;y<height;y++)for(let x=0;x<width;x++){const i=(y*width+x)*4,v=x<2?80:170;data.set([v,v,v,255],i)}const original=[...data];applyRealtimeEffects({data,width,height},{sharpenAmount:1,contrast:1.1,saturation:1,gamma:1});assert.notDeepEqual([...data],original);for(let i=3;i<data.length;i+=4)assert.equal(data[i],255)});
