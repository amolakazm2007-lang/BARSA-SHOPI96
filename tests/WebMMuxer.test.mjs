import test from'node:test';import assert from'node:assert/strict';import{muxIVFToWebM}from'../src/engine/WebMMuxer.js';
test('wraps IVF as WebM',async()=>{const h=new Uint8Array(32),v=new DataView(h.buffer);h.set([68,75,73,70],0);h.set([86,80,57,48],8);v.setUint16(6,32,true);v.setUint16(12,320,true);v.setUint16(14,180,true);v.setUint32(16,24000,true);v.setUint32(20,1000,true);v.setUint32(24,1,true);const fh=new Uint8Array(12);new DataView(fh.buffer).setUint32(0,3,true);const out=await muxIVFToWebM(new Blob([h,fh,new Uint8Array([1,2,3])]),{codec:'vp09.00.10.08',width:320,height:180,fps:24,duration:1/24}),bytes=new Uint8Array(await out.arrayBuffer());assert.equal(out.type,'video/webm');assert.deepEqual([...bytes.slice(0,4)],[0x1a,0x45,0xdf,0xa3]);assert.ok(new TextDecoder().decode(bytes).includes('V_VP9'))});
