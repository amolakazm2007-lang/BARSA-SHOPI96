# BARSA SHOPI v9.7 — Smart CacheGraph + Thermal Guard

هذه النسخة تبدأ مرحلة التثبيت العميق بدل إضافة محركات جديدة.

## المنفذ فعلياً
- CacheGraph بمفاتيح SHA-256 تعتمد على parent stage + stage id + settings + metadata.
- إعادة استخدام cache صالح لنفس المرحلة/الإعدادات داخل جلسة العمل بدل إعادة AI.
- rewind/undo لا يمسحان cache المفيد فوراً؛ يبقى قابلاً لإعادة الاستخدام إلى أن تُمسح الجلسة.
- Source fingerprint يعتمد على بيانات الملف وSHA-256 لأول/آخر 1MB لتقليل احتمال stale cache مع كلفة منخفضة.
- Android Thermal Guard عبر PowerManager: preflight قبل الرندر ومراقبة أثناءه.
- عند THERMAL_STATUS_SEVERE أو أعلى يتوقف الرندر مؤقتاً بدل خفض الجودة أو إسقاط الإطارات بصمت، ثم يكمل بعد عودة الحرارة لمستوى آمن.
- الإصدار 9.7.0 / Android versionCode 970.

## ما لم ندّعِ تنفيذه بعد
Progressive chunk resume الحقيقي للمراحل التي تعيد ترميز فيديو كامل ما زال يحتاج إعادة هيكلة أعمق لمسار VideoPipeline/encoder. البنية الحالية فيها checkpointing للإطارات، لكن لا نعتبر Heavy Apply chunk-resumable كاملاً بعد.
