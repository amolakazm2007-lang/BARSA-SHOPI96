# BARSA SHOPI v7.0 FINAL — Release Candidate

## Final hardening
- Android version unified to 7.0.0 / versionCode 70.
- Android runtime metadata unified to 7.0.0.
- GitHub APK artifact unified to BARSA-SHOPI-v7.0-debug.
- Java 17 retained in Android source and GitHub Actions.
- WebView uses hardware layer and high renderer priority on Android 8+.
- WebView lifecycle now resumes/pauses cleanly with Activity lifecycle.
- Automatic model provisioning retries transient failures up to three times with backoff and online recovery before fallback.
- Native ONNX tile path, WebGPU/WASM fallback, Blur-only MP4, model verification, temporal reconstruction, stabilization, color V3, Face V2, RIFE and MP4 validation remain enabled.

## Verification performed in this environment
- Source policy: PASS.
- Final audit: PASS (49 source modules / 14 critical runtime files).
- JavaScript syntax: PASS.
- Static UI IDs: 172 unique, 0 duplicates.
- Full Node suite: 119 tests total; 116 PASS.
- The 3 non-passing tests are environment dependency failures only:
  1. MediaInputEngine WebM probe requires `mediabunny` installed.
  2. NativeMP4Muxer browser test requires browser dependencies.
  3. StorageManager browser/OPFS test requires Playwright/browser dependencies.
  GitHub Actions runs `npm ci` and installs Chromium for the web CI workflow, so these are designed to run there.

## Important release note
This is the strongest source release candidate produced in-session. A zero-defect guarantee is not possible without installing the generated APK on the target POCO F6 and running real-device 1080p/4K, native NNAPI, long-render, RIFE, model-download and MediaCodec/WebView acceptance tests. The code is designed to fall back safely when a native/accelerated path is unsupported.
